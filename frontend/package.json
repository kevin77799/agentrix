{
  "name": "frontend",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@testing-library/dom": "^10.4.1",
    "@testing-library/jest-dom": "^6.8.0",
    "@testing-library/react": "^16.3.0",
    "@testing-library/user-event": "^13.5.0",
    "concurrently": "^9.2.1",
    "react": "^19.1.1",
    "react-dom": "^19.1.1",
    "react-scripts": "5.0.1",
    "serve": "^14.2.1",
    "web-vitals": "^2.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "dev": "concurrently \"npm start\" \"cd ../backend && .\\.venv\\Scripts\\activate && uvicorn main:app --reload\" \"cd ../backend/disease_agent && ..\\.venv\\Scripts\\activate && uvicorn app:app --port 8001\" \"cd ../backend/crop_agent && ..\\.venv\\Scripts\\activate && uvicorn app:app --port 8002\" \"cd ../backend/market_agent && ..\\.venv\\Scripts\\activate && uvicorn app:app --port 8003\" \"cd ../backend/weather_agent && ..\\.venv\\Scripts\\activate && uvicorn app:app --port 8004\" \"cd ../backend/resource_agent && ..\\.venv\\Scripts\\activate && uvicorn app:app --port 8005\""
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}